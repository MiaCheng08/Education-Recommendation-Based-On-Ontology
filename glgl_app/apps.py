from django.apps import AppConfig


class GlglAppConfig(AppConfig):
    name = 'glgl_app'


"""
The glgl_appConfig class is in this file
(that is'GlglAppConfig'),which meaning
the app's configuration class.
To let the Django know,wo should to designate
this class in the INSTALLED_APPS setting
"""
